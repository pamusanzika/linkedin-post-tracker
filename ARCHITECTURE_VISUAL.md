# 🎨 Profile Filtering - Visual Architecture

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                      DASHBOARD                              │ │
│  │                                                             │ │
│  │  URL: /dashboard?profileId=abc123                          │ │
│  │  ────────────────────────────────────────────              │ │
│  │                                                             │ │
│  │  ┌─────────────────┐         ┌─────────────────────────┐  │ │
│  │  │  Profile List   │         │    Posts Section        │  │ │
│  │  │  ─────────────  │         │    ──────────────       │  │ │
│  │  │                 │         │                         │  │ │
│  │  │  ┌───────────┐  │         │  ┌────────────────┐    │  │ │
│  │  │  │ Profile A │◄─┼─────┐   │  │ Filter Banner │    │  │ │
│  │  │  │ (Selected)│  │     │   │  │ Showing: A    │    │  │ │
│  │  │  └───────────┘  │     │   │  └────────────────┘    │  │ │
│  │  │                 │     │   │                         │  │ │
│  │  │  ┌───────────┐  │     │   │  ┌────────────────┐    │  │ │
│  │  │  │ Profile B │  │     └──►│  │  Post 1 (A)   │    │  │ │
│  │  │  └───────────┘  │         │  │  Post 2 (A)   │    │  │ │
│  │  │                 │         │  │  Post 3 (A)   │    │  │ │
│  │  │  ┌───────────┐  │         │  └────────────────┘    │  │ │
│  │  │  │ Profile C │  │         │                         │  │ │
│  │  │  └───────────┘  │         │                         │  │ │
│  │  └─────────────────┘         └─────────────────────────┘  │ │
│  │                                                             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                        EVENT FLOW                                 │
└──────────────────────────────────────────────────────────────────┘

1. USER CLICKS PROFILE A
   │
   ├─► handleProfileClick('abc123')
   │
   ├─► setSearchParams({ profileId: 'abc123' })
   │
   └─► URL changes to: /dashboard?profileId=abc123

                    ↓

2. URL STATE UPDATES
   │
   ├─► useSearchParams() detects change
   │
   ├─► selectedProfileId = 'abc123'
   │
   └─► selectedProfile = profiles.find(p => p._id === 'abc123')

                    ↓

3. REACT EFFECT TRIGGERS
   │
   ├─► useEffect([selectedProfileId]) runs
   │
   └─► loadPosts() called

                    ↓

4. API REQUEST
   │
   ├─► postsApi.getLatest(24, 'abc123')
   │
   └─► GET /api/posts/latest?hours=24&profileId=abc123

                    ↓

5. BACKEND PROCESSING
   │
   ├─► Look up profile by ID
   │   const profile = await TrackedProfile.findOne({
   │     _id: 'abc123',
   │     userId: req.user._id
   │   })
   │
   ├─► Add profileUrl to query
   │   query.profileUrl = profile.profileUrl
   │
   ├─► Execute filtered query
   │   const posts = await PostCache.find(query)
   │
   └─► Return filtered posts array

                    ↓

6. UI UPDATES
   │
   ├─► setPosts(filteredData)
   │
   ├─► Profile A card gets highlighted
   │
   ├─► Filter indicator appears
   │   "Showing posts by: Profile A"
   │
   └─► Posts feed shows only Profile A's posts

                    ↓

7. USER SEES RESULT
   ✓ URL persisted
   ✓ Filter active
   ✓ Correct posts displayed
```

---

## 🗂️ Component Hierarchy

```
<BrowserRouter>
  └─ <AuthProvider>
      └─ <Routes>
          └─ <Route path="/dashboard">
              └─ <Dashboard>                    ← State Management
                  ├─ searchParams              ← URL state
                  ├─ selectedProfileId         ← Derived from URL
                  ├─ selectedProfile           ← Computed from profiles
                  ├─ profiles                  ← Fetched from API
                  └─ posts                     ← Filtered from API
                  │
                  ├─ <ProfileList              ← User Interaction
                  │   profiles={profiles}
                  │   onProfileClick={handleProfileClick}
                  │   selectedProfileId={selectedProfileId}
                  │  >
                  │   └─ {profiles.map(profile =>
                  │       <ProfileCard         ← Clickable Card
                  │         onClick={() => onProfileClick(profile._id)}
                  │         className={selected ? 'highlighted' : ''}
                  │       />
                  │     )}
                  │
                  └─ <PostsSection>            ← Display Filtered Posts
                      ├─ <FilterIndicator      ← Shows active filter
                      │   profile={selectedProfile}
                      │   onClear={handleClearFilter}
                      │  />
                      └─ {posts.map(post =>
                          <PostCard            ← Individual Post
                            post={post}
                          />
                        )}
```

---

## 🌊 State Flow Chart

```
┌─────────────────────────────────────────────────────────────┐
│                    STATE MANAGEMENT                          │
└─────────────────────────────────────────────────────────────┘

URL (Source of Truth)
/dashboard?profileId=abc123
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
  useSearchParams()   Browser History
         │              (back/forward)
         │                 │
         ▼                 │
  selectedProfileId        │
    = 'abc123'             │
         │                 │
         ├─────────────────┘
         │
         ▼
  useEffect Dependency
         │
         ▼
  loadPosts(selectedProfileId)
         │
         ▼
  API Call with Filter
         │
         ▼
  Backend Query
         │
         ▼
  Filtered Posts Array
         │
         ▼
  setPosts(filteredData)
         │
         ▼
  UI Re-renders
         │
         ├─► Profile highlighted
         ├─► Filter banner shown
         └─► Posts displayed
```

---

## 🔐 Backend Query Logic

```
┌────────────────────────────────────────────────────────────┐
│              BACKEND FILTERING LOGIC                        │
└────────────────────────────────────────────────────────────┘

Request: GET /api/posts/latest?hours=24&profileId=abc123
                    │
                    ▼
            ┌───────────────┐
            │ Extract Params│
            └───────┬───────┘
                    │
        ┌───────────┴───────────┐
        │                       │
        ▼                       ▼
  hours = 24            profileId = 'abc123'
        │                       │
        │                       ▼
        │              ┌─────────────────┐
        │              │ Look up Profile │
        │              │ by _id and      │
        │              │ userId          │
        │              └────────┬────────┘
        │                       │
        │                       ▼
        │              profile.profileUrl
        │                       │
        └───────────┬───────────┘
                    │
                    ▼
            ┌────────────────┐
            │  Build Query   │
            │  ────────────  │
            │  {             │
            │   userId: X,   │
            │   timestamp: Y,│
            │   profileUrl: Z│ ← Added if profileId provided
            │  }             │
            └────────┬───────┘
                    │
                    ▼
          ┌──────────────────┐
          │ Execute MongoDB  │
          │ Query            │
          └────────┬─────────┘
                    │
                    ▼
          ┌──────────────────┐
          │ Return Filtered  │
          │ Posts Array      │
          └──────────────────┘
```

---

## 🎯 User Journey Map

```
┌──────────────────────────────────────────────────────────────┐
│                    USER JOURNEY                              │
└──────────────────────────────────────────────────────────────┘

START: Dashboard loads
│
├─► User sees all profiles
├─► User sees all posts
└─► URL: /dashboard

        │
        │ [User clicks Profile A]
        │
        ▼

Filter Applied
│
├─► URL changes: /dashboard?profileId=abc123
├─► Profile A card highlighted (blue border)
├─► Filter banner appears: "Showing posts by: Profile A"
└─► Posts update to show only Profile A's posts

        │
        │ [User clicks Profile B]
        │
        ▼

Filter Switched
│
├─► URL changes: /dashboard?profileId=xyz789
├─► Profile B highlighted, Profile A unhighlighted
├─► Filter banner updates: "Showing posts by: Profile B"
└─► Posts update to show only Profile B's posts

        │
        │ [User clicks "Show All Posts"]
        │
        ▼

Filter Cleared
│
├─► URL changes: /dashboard
├─► All profiles unhighlighted
├─► Filter banner disappears
└─► Posts show all posts from all profiles

        │
        │ [User refreshes page]
        │
        ▼

State Persists
│
├─► Current URL maintained
├─► If filtered: filter remains active
├─► If not filtered: shows all posts
└─► UI matches URL state

END: User continues browsing
```

---

## 📱 Responsive Behavior

```
┌─────────────────────────────────────────────────────────────┐
│                    DESKTOP VIEW                              │
└─────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  Header                                    Welcome | Logout   │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌────────────────────┐    ┌────────────────────────────┐   │
│  │  Tracked Profiles  │    │  Latest Posts              │   │
│  │  ────────────────  │    │  ──────────────            │   │
│  │  [Profile A] ✓     │    │  ┌────────────────────┐   │   │
│  │  [Profile B]       │    │  │ Showing: Profile A │   │   │
│  │  [Profile C]       │    │  └────────────────────┘   │   │
│  │                    │    │                            │   │
│  │  [Add Profile]     │    │  [Post 1]                  │   │
│  └────────────────────┘    │  [Post 2]                  │   │
│                            │  [Post 3]                  │   │
│                            └────────────────────────────┘   │
│                                                               │
└──────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│                    MOBILE VIEW                               │
└─────────────────────────────────────────────────────────────┘

┌───────────────────────────┐
│  Header            Logout │
├───────────────────────────┤
│                           │
│  Tracked Profiles         │
│  ─────────────────        │
│  [Profile A] ✓            │
│  [Profile B]              │
│  [Profile C]              │
│                           │
│  [Add Profile]            │
│                           │
├───────────────────────────┤
│                           │
│  Latest Posts             │
│  ──────────────           │
│  ┌─────────────────────┐ │
│  │ Showing: Profile A  │ │
│  │ [Show All]          │ │
│  └─────────────────────┘ │
│                           │
│  ┌─────────────────────┐ │
│  │ Post 1              │ │
│  │ ...                 │ │
│  └─────────────────────┘ │
│                           │
│  ┌─────────────────────┐ │
│  │ Post 2              │ │
│  │ ...                 │ │
│  └─────────────────────┘ │
│                           │
└───────────────────────────┘
```

---

## 🧩 Component Interaction Diagram

```
Dashboard
    │
    ├── State
    │   ├── searchParams (from URL)
    │   ├── selectedProfileId (from searchParams)
    │   ├── selectedProfile (computed)
    │   ├── profiles (from API)
    │   └── posts (from API, filtered)
    │
    ├── Handlers
    │   ├── handleProfileClick(id)
    │   │   └─► setSearchParams({ profileId: id })
    │   │
    │   └── handleClearFilter()
    │       └─► setSearchParams({})
    │
    ├── Effects
    │   └── useEffect([selectedProfileId])
    │       └─► loadPosts(selectedProfileId)
    │
    └── Render
        ├── <ProfileList>
        │   │
        │   ├── Props In
        │   │   ├── profiles
        │   │   ├── onProfileClick
        │   │   └── selectedProfileId
        │   │
        │   └── User Action
        │       └─► onClick → onProfileClick(id) → Dashboard updates URL
        │
        └── <PostsSection>
            │
            ├── Conditional Render
            │   └── {selectedProfile && <FilterIndicator />}
            │
            └── Posts List
                └── {posts.map(post => <PostCard />)}
```

---

## 🔄 Lifecycle Timeline

```
Timeline of Events:

T=0ms     Page Load
           │
           ├─► Dashboard component mounts
           ├─► useSearchParams reads URL
           └─► selectedProfileId = null (no query param)

T=10ms    Initial Data Load
           │
           ├─► loadProfiles() called
           └─► loadPosts(null) called (no filter)

T=500ms   Data Received
           │
           ├─► setProfiles([...]) triggers render
           └─► setPosts([...all posts]) triggers render

T=1000ms  User Clicks Profile A
           │
           └─► handleProfileClick('abc123')

T=1001ms  URL Updates
           │
           ├─► setSearchParams({ profileId: 'abc123' })
           ├─► Browser URL: /dashboard?profileId=abc123
           └─► useSearchParams updates

T=1002ms  State Recalculates
           │
           ├─► selectedProfileId = 'abc123'
           ├─► selectedProfile = profiles[0]
           └─► Component re-renders

T=1003ms  Effect Triggers
           │
           ├─► useEffect([selectedProfileId]) detects change
           └─► loadPosts('abc123') called

T=1500ms  Filtered Data Received
           │
           ├─► setPosts([...filtered]) triggers render
           └─► UI shows filtered posts

T=2000ms  User Clicks "Show All"
           │
           └─► handleClearFilter()

T=2001ms  Filter Cleared
           │
           ├─► setSearchParams({})
           ├─► URL: /dashboard
           ├─► selectedProfileId = null
           └─► loadPosts(null) → all posts
```

---

## 🎨 Visual States

```
┌──────────────────────────────────────────────────────────────┐
│                     STATE: NO FILTER                         │
└──────────────────────────────────────────────────────────────┘

URL: /dashboard

Profile Cards:        Posts Section:
┌──────────┐         ┌────────────────────┐
│ Profile A│         │ Latest Posts       │
└──────────┘         │ (Last 24 Hours)    │
┌──────────┐         │                    │
│ Profile B│         │ [Post from A]      │
└──────────┘         │ [Post from B]      │
┌──────────┐         │ [Post from C]      │
│ Profile C│         │ [Post from A]      │
└──────────┘         │ [Post from B]      │
                     └────────────────────┘


┌──────────────────────────────────────────────────────────────┐
│                  STATE: PROFILE A FILTERED                   │
└──────────────────────────────────────────────────────────────┘

URL: /dashboard?profileId=abc123

Profile Cards:        Posts Section:
┌──────────┐         ┌────────────────────┐
│ Profile A│ ✓       │ Latest Posts       │
│ (SELECTED)         │ (Last 24 Hours)    │
└──────────┘         │                    │
┌──────────┐         │ ┌────────────────┐│
│ Profile B│         │ │Showing: A      ││
└──────────┘         │ │[Show All Posts]││
┌──────────┐         │ └────────────────┘│
│ Profile C│         │                    │
└──────────┘         │ [Post from A]      │
                     │ [Post from A]      │
                     │ [Post from A]      │
                     └────────────────────┘
```

---

## 📊 Performance Metrics

```
┌──────────────────────────────────────────────────────────────┐
│                    PERFORMANCE ANALYSIS                       │
└──────────────────────────────────────────────────────────────┘

Scenario 1: Load Dashboard (No Filter)
───────────────────────────────────────
Component Render:    1x (initial)
API Calls:           2 (profiles + posts)
Database Queries:    2
Data Transferred:    All profiles + All posts
Render Time:         ~50ms

Scenario 2: Apply Filter
────────────────────────
Component Render:    1x (URL change triggers)
API Calls:           1 (filtered posts)
Database Queries:    1 (with profileUrl filter)
Data Transferred:    1 profile's posts only
Render Time:         ~30ms

Scenario 3: Switch Filter
─────────────────────────
Component Render:    1x (URL change triggers)
API Calls:           1 (new filtered posts)
Database Queries:    1 (with new profileUrl)
Data Transferred:    New profile's posts only
Render Time:         ~30ms

Scenario 4: Clear Filter
────────────────────────
Component Render:    1x (URL change triggers)
API Calls:           1 (all posts)
Database Queries:    1 (no profileUrl filter)
Data Transferred:    All posts
Render Time:         ~40ms

Scenario 5: Page Refresh (With Filter)
──────────────────────────────────────
Component Render:    1x (initial)
API Calls:           2 (profiles + filtered posts)
Database Queries:    2
Data Transferred:    All profiles + Filtered posts
Render Time:         ~50ms
```

---

**Last Updated:** November 24, 2025  
**Version:** 1.0.0
